<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev.parkingsystem">

    <application
        android:allowBackup="true"
        android:label="Parking System"
        android:theme="@style/Theme.ParkingSystem">
        <activity android:name=".RevenueActivity" />
        <activity android:name=".BookingsActivity" />
        <activity android:name=".ChangePricingActivity" />
        <activity android:name=".SlotMapActivity" />
        <activity android:name=".BookSlotActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".BookingSuccessActivity" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>